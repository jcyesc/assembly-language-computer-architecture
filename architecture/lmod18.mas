         ;                                       lmod18.mas
         ; Test module for linv1
         ; Link by itself
         ; Generates error message
         public x
         ld     x
         ld     x
         ld     x
         ld     x
         ld     x
         ld     x
         halt
x:       dw     0
